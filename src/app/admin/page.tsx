export default function AdminPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-gem-ink px-6">
      <p className="text-gem-mist/60">
        GemOps Admin — coming in iteration 1b. Run{" "}
        <code className="text-gem-gold">supabase/schema.sql</code> to seed the DB.
      </p>
    </main>
  );
}
